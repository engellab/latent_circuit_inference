from setuptools import setup, find_packages

setup(name='latent_circuit_inference',
      version='0.1',
      description='Inferring low dimensional circuits from RNNs dynamics',
      url='***',
      author='***',
      author_email='***',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)
